{% extends 'basis.html.twig' %}

{% block basis %}
    <h2 class="page-header">{{ 'edit'|trans }} {{ 'task'|trans }}</h2>
    {{ form_start(form) }}
    {{ form_widget(form) }}
    {{ form_end(form) }}
{% endblock %}